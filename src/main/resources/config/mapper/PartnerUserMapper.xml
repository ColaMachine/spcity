<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cola.machine.dao.PartnerUserMapper">
  <resultMap id="BaseResultMap" type="cola.machine.bean.PartnerUser">
      <id column="id" jdbcType="INTEGER" property="id" />
      <result column="userId" jdbcType="INTEGER" property="userId" />
      <result column="partnerId" jdbcType="BIGINT" property="partnerId" />
  </resultMap>
  <sql id="Base_Column_List">
    id ,userId ,partnerId 
   
  </sql>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from PartnerUser
    where id =   #{id,jdbcType=INTEGER}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from PartnerUser
    where id = #{id,jdbcType=INTEGER}
  </delete>
 <insert id="insert" useGeneratedKeys="true" keyProperty="id"   parameterType="cola.machine.bean.PartnerUser" >

    insert into PartnerUser (  <include refid="Base_Column_List" />)
    values (
#{id,jdbcType=INTEGER}
,#{userId,jdbcType=INTEGER}
,#{partnerId,jdbcType=BIGINT}
    )
  </insert>
   <insert id="insertSelective" parameterType="cola.machine.bean.PartnerUser" >
    insert into PartnerUser
    <trim prefix="(" suffix=")" suffixOverrides="," >
        <if test="id != null" >  
           id, 
        </if>  
        <if test="userId != null" >  
           userId, 
        </if>  
        <if test="partnerId != null" >  
           partnerId, 
        </if>  
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
        <if test="id != null" >  
          #{id,jdbcType=INTEGER},
        </if>  
        <if test="userId != null" >  
          #{userId,jdbcType=INTEGER},
        </if>  
        <if test="partnerId != null" >  
          #{partnerId,jdbcType=BIGINT},
        </if>  
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="cola.machine.bean.PartnerUser" >
    update PartnerUser
    <set >
        <if test="id != null" >  
             id=#{id,jdbcType=INTEGER},
        </if>  
        <if test="userId != null" >  
             userId=#{userId,jdbcType=INTEGER},
        </if>  
        <if test="partnerId != null" >  
             partnerId=#{partnerId,jdbcType=BIGINT},
        </if>  
    </set>
    where id = #{id,jdbcType=INTEGER}
  </update>
  <update id="updateByPrimaryKey" parameterType="cola.machine.bean.PartnerUser" >
    update PartnerUser
    set 
        <if test="id != null" >  
              id=#{id,jdbcType=INTEGER},
        </if>  
        <if test="userId != null" >  
              userId=#{userId,jdbcType=INTEGER},
        </if>  
        <if test="partnerId != null" >  
              partnerId=#{partnerId,jdbcType=BIGINT},
        </if>  
where id = #{id,jdbcType=INTEGER}
  </update>
  <select id="listByParams" parameterType="map" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from PartnerUser where 1=1
        <if test="id != null and id != '' ">  
           and id = #{id}
        </if>  
        <if test="userId != null and userId != '' ">  
           and userId = #{userId}
        </if>  
        <if test="partnerId != null and partnerId != '' ">  
           and partnerId = #{partnerId}
        </if>  
  </select>
   <select id="listByParams4Page" parameterType="map" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from PartnerUser where 1=1
        <if test="id != null and id != '' ">  
           and id = #{id}
        </if>  
        <if test="userId != null and userId != '' ">  
           and userId = #{userId}
        </if>  
        <if test="partnerId != null and partnerId != '' ">  
           and partnerId = #{partnerId}
        </if>  
  </select>
   <select id="countByParams" parameterType="map" resultType="java.lang.Integer">
    select 
    count(1) 
    from PartnerUser where 1=1
        <if test="id != null and id != '' ">  
           and id = #{id}
        </if>  
        <if test="userId != null and userId != '' ">  
           and userId = #{userId}
        </if>  
        <if test="partnerId != null and partnerId != '' ">  
           and partnerId = #{partnerId}
        </if>  
  </select>

</mapper>
